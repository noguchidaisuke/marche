<!--
<div class="row">
  <div class="col-xs-offset-3 col-xs-6"></div>
    <div class="panel panel-default">
      <div class="panel-heading">会員登録</div>
      <div class="panel-body">
        <%= form_for(@user) do |f| %>
          <div class="form-group">
            <%= f.label :name, 'お名前' %>
            <%= f.text_field :name, class: 'form-control' %>
          </div>

          <div class="form-group">
            <%= f.label :email, 'メールアドレス' %>
            <%= f.email_field :email, class: 'form-control' %>
          </div>

          <div class="form-group">
            <%= f.label :password, 'パスワード' %>
            <%= f.password_field :password, class: 'form-control' %>
          </div>

          <div class="form-group">
            <%= f.label :password_confirmation, 'パスワード（確認）' %>
            <%= f.password_field :password_confirmation, class: 'form-control' %>
          </div>

          <div class="text-right">
            <%= f.submit '登録する', class: 'btn btn-success' %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
<%=link_to 'Twitterでログイン', 'auth/twitter' %>
def content_for name
  
end
-->
<% content_for(:cover) do %>
<div class="sign-body">
  <div class="model-dialog text-center">
    <div class="main-section col-sm-6 col-sm-offset-3 col-lg-4 col-lg-offset-4">
      <div class="model-content">
        <div class="col-12 user-img">
          <i class="fab fa-gofore"></i>
        </div>
        <%= form_with(model: @user) do |f| %>
          <form class="col-12">
            <div class="form-group">
              <%= f.text_field :name, class: 'form-control',placeholder: "Username" %>
            </div>
            <div class="form-group email">
              <%= f.text_field :email, class: 'form-control',placeholder: "Email" %>
            </div>
            <div class="form-group password">
              <%= f.text_field :password, class: 'form-control',placeholder: "Password" %>
            </div>
            <div class="form-group password">
              <%= f.text_field :password_confirmation, class: 'form-control',placeholder: "Password(確認)" %>
            </div>
            <button type="submit" class="btn-signin"><i class="fas fa-sign-in-alt"></i>登録する</button>
            <p class="or">or</p>
            <button type="submit" class="btn-tweet-signin"><i class="fab fa-twitter"></i>Twitterでログイン</button>
          </form>
        <% end %>
      </div> <!-- end of cotent -->
    </div>
  </div>
</div>
<%end%>